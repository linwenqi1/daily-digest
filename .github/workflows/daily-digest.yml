name: Daily Digest

on:
  schedule:
    # Runs daily at 14:00 UTC, which is 22:00 Asia/Shanghai / Beijing time.
    - cron: "0 14 * * *"
  workflow_dispatch: {}  # allow manual trigger

permissions:
  contents: write

jobs:
  digest:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Fetch news sources
        run: python scripts/fetch_news.py

      - name: Enrich with DeepSeek
        env:
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
          DEEPSEEK_MODEL: deepseek-v4-pro
        run: python scripts/enrich_news.py

      - name: Generate markdown digest
        run: python scripts/generate_digest.py

      - name: Commit digest to repo
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git pull --rebase origin "${{ github.ref_name }}"
          git add 'digests/*.md'
          git diff --staged --quiet || git commit -m "Daily digest $(date +%Y-%m-%d)"
          git push origin HEAD:"${{ github.ref_name }}"

      - name: Send email
        env:
          EMAIL_USER: ${{ secrets.EMAIL_USER }}
          EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
          EMAIL_TO: ${{ secrets.EMAIL_TO }}
        run: python scripts/send_email.py
